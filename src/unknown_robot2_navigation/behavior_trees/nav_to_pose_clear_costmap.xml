<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">

    <Sequence name="NavigateToPoseWithClearCostmaps">

      <!-- Clear both costmaps one time before this goal starts -->
      <SingleTrigger name="ClearCostmapsOnce">
        <Sequence name="ClearBothCostmaps">
          <ClearEntireCostmap
            name="ClearGlobalCostmap"
            service_name="global_costmap/clear_entirely_global_costmap"/>

          <ClearEntireCostmap
            name="ClearLocalCostmap"
            service_name="local_costmap/clear_entirely_local_costmap"/>
        </Sequence>
      </SingleTrigger>

      <!-- Normal Nav2 navigation behavior -->
      <PipelineSequence name="NavigateWithReplanning">

        <RateController hz="1.0">
          <ComputePathToPose
            goal="{goal}"
            path="{path}"
            planner_id="GridBased"/>
        </RateController>

        <FollowPath
          path="{path}"
          controller_id="FollowPath"/>

      </PipelineSequence>

    </Sequence>

  </BehaviorTree>
</root>
